<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cl.uct.cedest.conteocelulas">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.NinosEdadActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Controller.ConstantesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Controller.ConteoActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Controller.BaseActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Controller.AtlasActivity" />
        <activity android:name=".Controller.CaracteristicaMain" />
        <activity android:name=".Controller.CaracteristicaPlaqueta" />
        <activity android:name=".Controller.CaracteristicaLeucocitos" />
        <activity android:name=".Controller.CaracteristicaHematies" />
        <activity android:name=".Controller.ResultadoFinalActivity"></activity>
    </application>

</manifest>